const auth = require('./modules/auth');
const Auth2 = require('./modules/auth2');
const contacts = require('./modules/contacts');
const calendar = require('./modules/calendar');

module.exports = {
    auth,
    Auth2,
    calendar,
    contacts    
}
